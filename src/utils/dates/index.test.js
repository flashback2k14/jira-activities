import { expect, jest, describe, it } from "@jest/globals";
import {
  getCurrentEndDate,
  getCurrentStartDate,
  getDatesBetween,
} from "./index.js";

describe("dates", () => {
  it("should exists", () => expect(true).toBeTruthy());
});
